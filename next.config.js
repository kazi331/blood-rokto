/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // experimental: {
  //   appDir: true
  // },
  images: ['flagcdn.com']
}

module.exports = nextConfig
